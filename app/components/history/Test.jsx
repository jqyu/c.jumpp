import { React, View } from  'reapp-kit';

export default class extends React.Component {
  render() {
    return(
          <View {...this.props}>
            hey this is a test
          </View>
        );
  }
}
